import { render, screen } from "@testing-library/react";
import React from "react";

describe("Basic Test", () => {
  it("this is just temporary so github actions can pass", () => {
    expect(1 + 1).toBe(2);
  });
});
